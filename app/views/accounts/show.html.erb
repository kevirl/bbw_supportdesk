<div class="page-header">

<h1>Account Information</h1>

</div>

<div class="panel panel-primary">
  <!-- Default panel contents -->
  <div class="panel-heading" align"center">SupportDesk Account Information</div>
  <div class="panel-body">


<table class="table">
  <tr>
      <th>Account ID</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Email</th>
      <th>Phone</th>
  </tr>
  
  </div>

      <tr>
        <td><%= @user.id %></td>
        <td><%= @user.fname %></td>
        <td><%= @user.lname %></td>
        <td><%= @user.email %></td>
        <td><%= @user.phone %></td>
      </tr>

</table>

<div class="panel-footer">
  <%= link_to 'Create Ticket', new_ticket_path, class: 'btn btn-success' %>
  </div>

  </div>